Now think hard and write elegant code the completes this.

Do not add backwards compatiility unless explicitly requested.

After every code block you write, lint, compile and write 
corresponding tests and run them bevore writing the next
code block.  